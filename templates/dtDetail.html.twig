<table id="example" class="display" cellspacing="0" width="100%">
    <thead>
        <tr>
            
            {% for item in header %} 
                <th>{{ item }}</th>                    
            {% endfor %}
        </tr>
    </thead>
    <tbody> 
            {% for item in result %}
            <tr>
                <td>{{ item.p }}</td>
                <td>{{ item.o }}</td>                
            </tr>
        {% endfor %}                        
    </tbody>
</table>
  
{{ childResult }}

{{ childHeader }}

    <div>
        
        Child Resources
    
        <table id="example" class="display" cellspacing="0" width="100%">
            <thead>
                <tr>

                    {% for cheader in childHeader %} 
                        <th>{{ cheader }}</th>                    
                    {% endfor %}

                    <th>Details</th>
                    <th>Edit</th>
                </tr>
            </thead>
            <tbody> 
                    {% for citem in childResult %}
                    <tr>
                        <td>{{ citem.uri }}</td>                        
                        <td><a href="{{ citem.detail }}">Details</a></td>
                        <td><a href="{{ citem.edit }}">edit</a></td>
                    </tr>
                {% endfor %}                        
            </tbody>
        </table>
    
    </div>    
    
    

